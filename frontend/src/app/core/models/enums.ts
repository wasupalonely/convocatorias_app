// Valores literales en espanol: se envian/reciben tal cual; solo se "embellecen" al mostrarlos.
export type Role = 'ADMINISTRADOR' | 'DOCENTE' | 'ESTUDIANTE';

export type UserStatus = 'ACTIVO' | 'INACTIVO';

export type CallStatus = 'BORRADOR' | 'PUBLICADA' | 'CERRADA';

export type ApplicationStatus = 'PENDIENTE' | 'APROBADA' | 'RECHAZADA';
